<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Metadados -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Estilos CSS e Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/default.css"> <!-- Estilo padrão -->
    <link rel="stylesheet" href="styles/login.css"> <!-- Estilo específico -->
    <!-- Título da página -->
    <title>Login</title>
</head>

<body class="bg-white">
    <!-- Logo -->
    <img class="img-fluid mx-auto d-block" src="images/logo.png" alt="Logo" width="400px">
    <!-- Formulário de login -->
    <div class="d-flex align-items-center justify-content-center flex-column">
        <form class="col-xl-3 shadow rounded p-5" action="/login" method="post">
            <!-- Título do formulário -->
            <h1 class="text-center fw-bold display-5 mb-4 color-orange">Login</h1>
            <!-- Campo de usuário -->
            <div class="form-group mb-3">
                <label for="user">Usuário</label>
                <input id="user" name="user" type="text" class="form-control" />
            </div>
            <!-- Campo de senha -->
            <div class="form-group mb-3">
                <label for="password">Senha</label>
                <input id="password" name="password" type="password" class="form-control" />
            </div>
            <!-- Exibe mensagem de erro, se houver -->
            {{#error}}<div class="alert alert-danger" role="alert">{{.}}</div>{{/error}}
            <!-- Botão de login -->
            <button type="submit" class="btn btn-lg col-12 bg-orange">Entrar</button>
            <!-- Link para voltar para a tela inicial -->
            <a href="/" class="text-decoration-none color-orange">Voltar para a tela inicial</a>
    </div>
    </form>
</body>

</html>