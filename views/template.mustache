<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/default.css"> 
    <link rel="stylesheet" href="styles/template.css"> 

    <!-- Título -->
    <title>{{ title }}</title>
</head>

<body class="bg-white">
    <div class="text-center">
        <!-- LOGO-->
        <img class="img-fluid mx-auto d-block" src="images/logo.png" alt="Logo" width="250px">
    </div>

    <section class="bg-white d-flex flex-column align-items-center justify-content-center"> 
        <h1 class="text-center color-orange fw-bold mb-3">{{ title }}</h1>
        <div class="content-div bg-white rounded-3 shadow p-4" style="width: 50%;">
            <p class="content-text text-center">{{ content }}</p>
        </div>
        
        <!-- Botões de ação -->
        <div class="mt-3">
            <a href="/edit/{{ url }}" class="btn btn-warning text-white" style="background-color: #ff9800; border-color: #ff9800;">
                Editar
            </a>
            <form action="/delete/{{ url }}" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja excluir esta receita?');">
                    Excluir
                </button>
            </form>
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+O2PH+z6k47ZfRXcWft7C3v1VDPp" crossorigin="anonymous"></script>
</body>
</html>
